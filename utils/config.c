#include "../include/config.h"
#include "../utils/logger.h"
#include <stdio.h>

void load_config() {
    log_info("Carregando configurações do sistema...");
    // Simulação de carregamento de arquivo config.json (fake)
    printf("Configurações carregadas com sucesso.\n");
}
